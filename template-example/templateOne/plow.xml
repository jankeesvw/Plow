<?xml version="1.0" encoding="UTF-8"?>
<plow>
	<replaces>
		<replace fieldname="Project Name" replace="${project_name}" default="Project Name" />
		<replace fieldname="Author" replace="${author}" default="jankees [at] base42.nl" />
		<replace fieldname="Index.html" replace="index.html" default="project.html" />		
		<replace fieldname="filename" replace="filename" default="updated_filename" />				
	</replaces>
</plow>