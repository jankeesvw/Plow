<?xml version="1.0" encoding="UTF-8"?>
<plow>
	<replaces>
		<replace label="Project Name" replace="${project_name}" default="Project Name" />
		<replace label="Author" replace="${author}" default="jankees [at] base42.nl" />
		<replace label="Index.html" replace="index.html" default="project.html" />		
		<replace label="filename" replace="filename" default="updated_filename" />				
	</replaces>
</plow>